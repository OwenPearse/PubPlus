EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_AUTH_REDIRECT_SCHEME=pubplus

# Use only frontend-safe EXPO_PUBLIC_ values here.
# Never put Supabase service role keys, database passwords, or demo credentials in Expo env files.
# Social sign-in also requires Supabase provider setup and Google/Facebook/Apple dashboard config.
# The redirect scheme is used for native deep-link callbacks.
# Expo web OAuth returns to the current app origin plus /auth/callback.

# Optional/Replit-specific variable retained for compatibility:
EXPO_PUBLIC_DOMAIN=your-replit-domain.replit.dev
