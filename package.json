{
  "name": "pubplus",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "portal:dev": "corepack pnpm --dir portal_web dev",
    "portal:build": "corepack pnpm --dir portal_web build",
    "portal:test": "corepack pnpm --dir portal_web test",
    "portal:typecheck": "corepack pnpm --dir portal_web typecheck"
  }
}
